{
  "name": "youtube-clipper-webhook-examples",
  "version": "1.0.0",
  "description": "Webhook integration examples for YouTube Video Clipper actor",
  "type": "module",
  "scripts": {
    "webhook-handler": "node webhook-handler.js",
    "run-actor": "node run-with-webhook.js",
    "test-local": "echo 'Start webhook-handler first, then expose with ngrok, then run-actor'"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "ngrok": "^5.0.0-beta.2"
  }
}
